
const devArea = {
    version: "01ab",
    production: true,
};

const cliente = {
    device: "web",
};

// Exporta apenas o que deve ser acessado por outros módulos
module.exports = {
    devArea, cliente
};